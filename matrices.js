let matrix = [
  [10, 20, 30],
  [40, 50, 60],
];

for (let filas = 0; filas < matrix.length; filas++) {
  for (let columnas = 0; columnas < matrix[filas].length; columnas++) {
    console.log(matrix[filas][columnas]);
  }
}
